/**
 * 
 * 1. Open modal by pressing the button.
 * 2. Get value of given date ---> check if it is valid
 * 3. Get value of given name
 * 4. Get value of given email ---> check if it is valid
 * 5. Show fetched times for chosen challenge --> take selected time
 * 6. Show fetched number of participants for chosen challenge ---> take selected value
 * 7. Send challengeId, date, time, name, email, paricipants for reservation
 * 8. Close modal
 * 9. If clicked anywhere else not on modal close/remove modal.
 * 
 */

/*
import loadData from './loadData.js';
import ladTimes from './loadTimes.js'
import reserveTime from './sendReservation.js'; */

/* const input1 = document.querySelector('modal-date'); 
const input2 = document.querySelector('modal-name');
const input3 = document.querySelector('modal-email');
const input4 = document.querySelector('modal-times');
const input5 = document.querySelector('modal-participants');

const buttonStep1 = document.querySelector('button-step1');
const buttonStep2 = document.querySelector('button-step2');

buttonStep1.addEventListener('click', async function (input1) {


        if (input1.value) {
        const times = await loadTimes

        }
        else alert('Date have wrong format');
});

function bookingStep2() {

    input2.value //name
    input3.value // email
    input4.value // fetch times
    input5.value // fetch participants
} */

/*console.log("An example to validate the date");
let isValidDate = Date.parse('05/11/22');
if (isNaN(isValidDate)) {
  console.log("Not a valid date format.");
}
else{
    console.log("Valid date format.");
} */ 